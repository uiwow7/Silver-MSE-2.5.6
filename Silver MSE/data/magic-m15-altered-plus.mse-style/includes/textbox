chop_top := {if styling.chop_top == "" then 0 else if comma_count(styling.chop_top) == "," or comma_count(styling.chop_top) == ",," then split_text(match:",", styling.chop_top).0 else styling.chop_top}
chop_bot := {if comma_count(styling.chop_top) == ",," then split_text(match:",", styling.chop_top).1 else if styling.chop_bottom == "" then 0 else styling.chop_bottom}
base_top_of_textbox := { if is_map() then 340 else if is_halfart() then 413 else if is_full_art() then 359 else if is_mutate() and not is_prototype() then 395 else if is_prototype() and not is_mutate() then 385 else if is_prototype() and is_mutate() then 453.2 else if is_deck() then 60 else if use_porthole() then 369 else if use_styles() then 378 else if is_saga() then 0 else 327 }
unchopped_top_of_textbox := { base_top_of_textbox() + textbox_size_adjusted() }
top_of_textbox := { unchopped_top_of_textbox() + to_int(chop_top()) }
unchopped_bottom_of_textbox := { if is_map() then 475 else if use_porthole() then 470 else if is_saga() then 0 else 481 }
bottom_of_textbox := { unchopped_bottom_of_textbox() - to_int(chop_bot()) }
left_of_textbox := { if (card.level_0 != "" or card.level_1 != "" or card.level_2 != "") then 75 else if is_map() then 40 else if is_focus2() or is_adventure2() or is_omen2() then 27 else if is_focus() then 190 else if is_omen() then 187 else if is_adventure() then 200 else if use_porthole() then 54 else 29 }
width_of_textbox := { if is_leveler() then 0 else (if(card.level_0 != "" or card.level_1 != "" or card.level_2 != "") then -46 else 0) + if is_map() then 287 else if is_focus2() or is_focus() then 154 else if is_omen2() or is_omen() then 156 else if is_adventure2() then 157 else if is_adventure() then 143 else if use_porthole() then 286 else 314 }
height_of_textbox := { unchopped_bottom_of_textbox() - base_top_of_textbox() }

typeline_top := { (if is_map() then 297.5 else if is_glitch() then 299 else if is_halfart() then 382 else if is_clear() then 294 else if is_thbland() or is_saga() then 444 else if is_full_art() then 329 else if is_deck() then 700 else if use_porthole() then 290 else if use_styles() then 350 else 296) + ( shrink_type() + textbox_size_adjusted() ) }