pt:
	z index: 150
	left: {if is_leveler() and not lone_pt() then leveler_pt_left() else 286}
	top: { if is_leveler() and not lone_pt() then (l1_center() - 14 + leveler_pt_top_offset()) else if is_halfart() or is_thbland() then 479 else if is_map() then 467 else if use_porthole() then 463 else if is_old() then 476 else 469}
	width: {if starting_loyalty() and not is_leveler() then 0 else leveler_pt_width()}
	height: {leveler_pt_height()}
	alignment: center middle shrink-overflow
	font:
		name: {pt_font()}
		italic name: {pt_font_italic()}
		size: {pt_font_size()}
		color: {pt_font_color()}
		separator color: red
		shadow color: {if is_shifted() and not black_name() or is_fnm() or is_old() then rgb(0,0,0) else rgba(0,0,0,0)}
		shadow displacement x: -1
		shadow displacement y: 1

pt 2:
	z index: 160
	left: {leveler_pt_left()}
	top: {if is_prototype() and not is_mutate() then 355 else if is_prototype() and is_mutate() then 422.2 else (l2_center() - 12 + leveler_pt_top_offset())}
	width: {if is_leveler() and not lone_pt() or card.pt_2 != "" then leveler_pt_width() else if is_prototype() then leveler_pt_width() else 0}
	height: {leveler_pt_height()}
	alignment: center middle shrink-overflow
	font:
		name: {pt2_font()}
		italic name: {pt2_font_italic()}
		size: {pt2_font_size()}
		color: {pt2_font_color()}
		separator color: red
		shadow color: {if is_prototype() then rgb(0,0,0) else rgba(0,0,0,0)}
		shadow displacement x: 1
		shadow displacement y: 1
pt 3:
	z index: 140
	left: {leveler_pt_left()}
	top: {(l3_center() - 12 + leveler_pt_top_offset())}
	width: {if is_leveler() and not lone_pt() or card.pt_3 != "" then leveler_pt_width() else 0}
	height: {leveler_pt_height()}
	alignment: center middle shrink-overflow
	font:
		name: Beleren Bold
		size: {if is_map() then 14 else if space_leveler() then 13 else 16}
		color: {if is_shifted() and not black_name() or is_fnm() or is_inverted() or space_leveler() or is_familiar() or is_paradox() then "white" else if is_vehicle() and (not is_legend() or chosen(styling.other_options, choice:"brown legendary vehicle pt")) then "white" else "black"}
		separator color: red
		shadow color: {if is_shifted() and not black_name() or is_fnm() then rgb(0,0,0) else rgba(0,0,0,0)}
		shadow displacement x: -1
		shadow displacement y: 1
pt 7:
	z index: 140
	left: {leveler_pt_left()}
	top: {(l4_center() - 12 + leveler_pt_top_offset())}
	width: {if is_leveler() and not lone_pt() or card.pt_7 != "" then leveler_pt_width() else 0}
	height: {if card_style.level_4_text.height < 10 and card.pt_7 == "" then 0 else leveler_pt_height()}
	alignment: center middle shrink-overflow
	font:
		name: Beleren Bold
		size: {if is_map() then 14 else if space_leveler() then 13 else 16}
		color: {if is_shifted() and not black_name() or is_fnm() or is_familiar() then "white" else if is_vehicle() and (not is_legend() or chosen(styling.other_options, choice:"brown legendary vehicle pt")) then "white" else "black"}
		separator color: red
		shadow color: {if is_shifted() and not black_name() or is_fnm() then rgb(0,0,0) else rgba(0,0,0,0)}
		shadow displacement x: -1
		shadow displacement y: 1
loyalty:
	z index: 170
	left: 326
	top: 468
	width: {if starting_loyalty() then 13.16 else 0}
	height:	34.63
	alignment: center middle
	font:
		name: Beleren Bold
		size: 14
		color: white